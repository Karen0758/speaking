# 小星 · 产品需求文档 v2

> 状态：草稿，待确认
> 上一版：PRD.md（产品名「开麦」，v0.3）
> 本版新增：命名与定位、场景练习模块、题库扩充规划

---

## 一、产品定位

### 1.1 一句话定义

小星是一个陪你每天练一分钟口语表达的网页工具。抽一道题或一张场景卡，对着麦克风讲六十秒，讲完立刻拿到复盘。

### 1.2 核心信念

表达能力不是天赋，是可以通过刻意练习提升的技能。

大多数人知道自己「不太会讲」，但缺的不是道理，是一个低压力、有反馈、能天天来的练习场。健身有健身房，写作有日更，口语表达一直没有对应的东西。小星要做的就是这个。

### 1.3 与同类产品的区别

| 类型 | 它们解决什么 | 小星不同在哪 |
|---|---|---|
| 演讲课 / 训练营 | 系统方法论 | 不教理论，只给练习场和即时反馈 |
| 面试题库 App | 题目和范文 | 不给标准答案，让你自己先讲出来 |
| AI 陪练对话产品 | 多轮对话模拟 | 单向输出为主，一分钟一轮，门槛更低 |
| 录音复盘工具 | 记录 | 记录之外还给结构化评分和示范重讲 |

关键差异是**开口成本**。小星的设计目标是从打开页面到开始讲话不超过十五秒。

---

## 二、命名与 Slogan

### 2.1 命名方向

角色（黄色圆身体、手持星星棒、会张嘴唱歌的小人）已经是这个产品辨识度最高的资产。名字应该向角色靠拢，让「产品」和「陪练伙伴」是同一个东西。

### 2.2 候选名

| 候选 | 含义 | 优点 | 风险 |
|---|---|---|---|
| **小星**（推荐） | 角色即产品 | 两字好念好记，可直接人格化（「小星在听」「问问小星」），中英文都短 | 「小星」较通用，商标和搜索区分度需要靠视觉建立 |
| 星星在听 | 强调倾听而非评判 | 温度最高，直接传达产品态度，天然抗「被打分」的焦虑 | 四字偏长，做 App 名和域名都吃力，更适合做 Slogan |
| 开麦星 | 保留旧名资产 | 延续已有认知，「开麦」的动作感强 | 三字略拗口，「星」像后缀而非本体 |
| 有星听 | 谐音「有心听」 | 巧妙，有记忆点 | 需要解释才懂，第一次听会误解为「有星星听」 |
| 星星棒 | 角色道具 | 具象、有魔法感 | 更像功能名或周边名，作为产品名指向不清 |

**建议采用「小星」**，把「星星在听」降级为副标题或场景文案使用。

域名与商标可用性需要你自行核实，我没有能力查询实时注册状态。

### 2.3 Slogan

主 Slogan：

> **讲给小星听**

延续现有首页文案「练一分钟，讲给自己听」的句式，把「自己」换成有形象的陪伴者，孤独感变成陪伴感，同时完成了产品名的植入。

配套使用的副文案，按场合选用：

- 首页副标题：每天一分钟，把想说的话讲顺
- 练习中：慢慢说，不着急
- 复盘页：已经开口了，就是进步
- 分享卡片：我今天对小星讲了一分钟

### 2.4 语气准则

所有面向用户的文案遵守三条：

1. **不评判**。用「这次比上次稳」，不用「你的表达能力较弱」。
2. **不夸张**。用「讲得挺清楚」，不用「太棒了」「完美」。
3. **不说教**。建议要具体可执行，「开头先给结论」优于「注意逻辑结构」。

---

## 三、目标用户

### 3.1 优先级

**第一优先：表达力提升者（泛人群）**

日常聊天容易卡壳、开会不敢发言、讲事情没条理的人。人数最多，动机最软，所以产品必须靠**习惯机制**留住他们，而不是靠专业度。

这决定了几件事：

- 首屏不能先问「你要面试还是汇报」，应该有一个「今天随便练一道」的零决策入口
- 连续练习天数、累计分钟数这类习惯指标要做，且要做得显眼
- 场景专业度可以往后放，但每日提醒和轻量成就感不能少

**第二优先：职场人（汇报、跨部门沟通）**

使用频次高，场景明确，是场景练习模块的主要受益者。

**第三优先：求职者**

需求最强、付费意愿最高，但有明确截止日期，用完即走，留存差。作为高价值但非主线的人群运营。

### 3.2 用户画像

**画像 A：小雨，26 岁，运营**

开周会时脑子里想得清楚，一开口就乱。不想报课，也没有人陪练。希望有个地方能自己偷偷练，练砸了没人知道。

她需要的是：低门槛、不被评判、能看到自己在变好。

**画像 B：陈工，31 岁，后端开发**

技术没问题，但每次向产品和老板解释技术方案，对方都听不懂。晋升答辩讲砸过一次。

他需要的是：带具体背景的场景练习，以及「受众意识」这个维度的针对性反馈。

---

## 四、功能架构

### 4.1 全局结构

```
小星
├── 登录页（新增）
│   ├── 邮箱输入
│   ├── 验证码输入
│   └── 未登录可试用通用题库
├── 建档页（新增，首次进入）
│   ├── 角色 / 行业 / 年限（单选标签）
│   ├── 最想改善什么（多选，最多 3 项）
│   └── 补充一句（可跳过）
├── 首页
│   ├── 今天随便练一道（零决策入口，走通用题库）
│   ├── 从我的题库抽一张（主入口，新增）
│   ├── 选场景：工作汇报 / 求职面试 / 观点闲聊
│   └── 连续天数与本周记录（新增）
├── 题库页（新增）
│   ├── 场景卡列表，默认全部 Approve
│   ├── 逐张开关，批量整理
│   ├── 「先不看了，直接开练」跳过
│   └── 轮次标记与剩余未练数
├── 抽题页
│   ├── 难度选择：简单 / 中等 / 困难（默认按近期表现推荐）
│   ├── 随机抽题（老虎机动画）
│   ├── 换题
│   ├── 跳过，别再出这题（新增，练习中剔除）
│   └── 求职档案（面试场景专属）
│       ├── 目标岗位
│       └── 简历上传，AI 提炼重点
├── 场景卡页（新增）
│   ├── 画布式布局：卡点居中占整列，限制横贯底部
│   ├── 电报体要点，一屏看完，无需点击展开
│   ├── 三段式提纲（仅简单档）
│   ├── 八格画布：什么场合 / 已经发生 / 你已知的 / 难在这里 / 你是谁 / 对面是谁 / 要拿到 / 不能做
│   ├── 准备时间（可选 30 秒）
│   └── 进入练习
├── 账号页（新增）
│   ├── 导出全部数据
│   ├── 自带密钥（可选，讯飞与 AI）
│   └── 注销并删除
├── 记录页（新增）
│   ├── 连续练习天数
│   ├── 本周练习分钟数
│   ├── 最近十次平均分折线（按难度分色）
│   ├── 按题目回看：题干 / 转写 / 评分 / 建议
│   └── 高频口癖词排行
├── 练习页
│   ├── 倒计时（简单中等 60 秒，困难 90 到 120 秒）
│   ├── 讯飞实时语音转写
│   ├── 口语词实时高亮
│   ├── AI 卡壳提示（简单 2.5 秒 / 中等 3.5 秒触发，困难档不给）
│   ├── 麦克风音量可视化
│   └── 场景卡折叠常驻（新增，随时可回看背景）
└── 复盘页
    ├── 总评一句话（必须点到这次的具体特征）
    ├── 逐句点评：引用原话，指出问题与亮点，给可替换的说法
    ├── 诊断：卡点 / 目标 / 听众要点 / 限制 四项判定
    ├── 追问预演：对方接下来会问什么，每条追溯到你的哪句话
    ├── AI 评分（分维度）与难度标签
    ├── 口癖词统计、字数、语速
    ├── 示范重讲
    └── 完整转写文本
```

### 4.2 陪练角色

小星以可拖动的悬浮伙伴形式常驻页面，表情由应用真实状态驱动，不是循环播放的装饰动画。

| 应用状态 | 角色状态 | 文案 |
|---|---|---|
| 首页停留 | idle | 今天想练哪一种？ |
| 抽题动画中 | cast（施法，星星喷射） | 抽题中 |
| 抽到题 | cheer | 就这题吧 |
| 录音中，检测到说话 | sing（嘴巴随音量开合） | 我在听 |
| 录音中，安静 | idle | 慢慢说，不着急 |
| 停顿触发 AI 提示 | shy | 要不要提个醒 |
| 剩余不足 10 秒 | surprise | 还剩 N 秒 |
| 复盘生成中 | cast | 正在看你刚才说了什么 |
| 平均分 8 以上 | cheer | 这次讲得真好 |
| 平均分 6 到 7 | wink | 比上次稳 |
| 平均分 5 以下 | shy | 已经开口了，就是进步 |
| 30 秒无操作 | sleepy | 我先眯一会儿 |

角色右上角可关闭，关闭状态写入 localStorage。这是产品原则「不打扰」的体现：可爱不能变成负担。

---

## 五、个人题库（新增模块）

### 5.1 要解决的问题

现有三十道题都是「裸题」，只有一句题干，而且对所有人都一样。

两个问题叠在一起：

**第一，裸题无法让人开口。**「向老板汇报本月项目进展和遇到的最大困难」这句话，用户不知道自己是谁、公司在做什么、进展到底是多少、困难究竟是什么。于是要么编，要么卡住。编造消耗的是想象力，不是表达力，练习的负担被放错了地方。

**第二，通用题库对个人无效。**做设计的和做销售的，卡点完全不同。一个通用题库要么泛到没用，要么就得做得极大而用户找不到自己那一块。

解法是：**让用户先描述自己，AI 据此生成一批只属于他的场景卡。**

### 5.2 完整流程

```
首次进入
   │
   ├─ 1. 结构化建档（4 个选择 + 1 个可选输入，约 30 秒）
   │
   ├─ 2. 生成第一批 12 张场景卡（约 15 秒，流式逐张出现）
   │      └─ 后台继续补到 50 张
   │
   ├─ 3. 审核页（全部默认 Approve）
   │      ├─ 逐张浏览，点掉不合适的
   │      └─ 或者点「先不看了，直接开练」跳过
   │
   ├─ 4. 抽题练习
   │      └─ 抽到不合适的题：「跳过，别再出这题」当场剔除
   │
   ├─ 5. 练完自动复盘，写入记录
   │
   └─ 6. 剩余未练题数 < 5 时，静默生成第二轮
```

关键设计：**审核有两条路径，但都不是必经之路**。审核页保留给想掌控的人，跳过按钮留给想马上开始的人，练习中的剔除按钮兜住所有漏网的题。这样既满足控制欲，又不违背「十五秒开口」。

### 5.3 结构化建档（内置提示词）

不能让用户自由描述「我最大的困扰」。多数人会写「我不太会说话」，这对生成毫无信息量。

改成以选择为主、输入为辅：

| 字段 | 形式 | 选项 |
|---|---|---|
| 你的角色 | 单选标签 | 产品 / 研发 / 设计 / 运营 / 市场 / 销售 / 人力 / 财务 / 管理者 / 学生 / 自由职业 / 其他 |
| 行业 | 单选标签 | 互联网 / 金融 / 教育 / 医疗 / 制造 / 咨询 / 政府事业 / 零售消费 / 其他 |
| 工作年限 | 单选 | 学生或应届 / 1-3 年 / 3-5 年 / 5-10 年 / 10 年以上 |
| 最想改善什么 | 多选，最多 3 项 | 见下 |
| 补充一句 | 自由输入，可跳过 | 占位符：「下周要跟老板提涨薪」 |

**「最想改善什么」的选项**（这批文案是整个功能的核心资产，每一项直接对应一类卡点）：

- 一紧张就语无伦次
- 说话绕半天说不到重点
- 会上不敢开口，或者开口没人听
- 讲专业的事，外行听不懂
- 一被追问就慌
- 不知道怎么拒绝别人
- 坏消息说不出口
- 讲自己的经历讲得干巴巴
- 闲聊时接不上话
- 临时被点名发言就大脑空白

用户勾三个，AI 拿到的画像是「互联网 / 产品 / 3-5 年 / 讲专业的事外行听不懂 + 一被追问就慌 + 坏消息说不出口」。这比任何一段自述都可靠。

### 5.4 场景卡数据结构

```js
{
  id: "sc_a1b2c3",
  round: 1,                    // 第几轮生成
  approved: true,              // 默认 true
  difficulty: "medium",        // easy / medium / hard
  scenario: "汇报",            // 汇报 / 面试 / 闲聊
  painPoint: "被追问就慌",      // 对应建档时勾选的卡点
  title: "上线前一天被问到数据来源",

  situation: "周四下午的上线评审会，你讲完方案后",
  role: "你是这个功能的产品负责人，入职一年半",
  audience: "技术负责人，他要为线上稳定性负责，不接受含糊的答案",
  facts: "你在方案里写了「预计提升转化 15%」，这个数字来自上季度的一次小范围试验",
  assets: "那次试验只跑了三天，还正好赶上一次大促，平时未必有这个效果",
  blocker: "他连问三个「这个数是怎么来的」，你答不上来但又不能说不知道",
  goal: "承认数据不够扎实，同时保住方案本身的合理性，争取按期上线",
  constraint: "两分钟内，不能把责任推给数据同学",

  // 电报体要点，12 字以内，画布上显示的就是这一份
  brief: {
    situation: "健身房门口、你正要走",
    role: "月卡会员、来了两个月",
    audience: "教练小李、这月差两单",
    facts: "前两次都说「再想想」",
    assets: "还得继续来这家",
    blocker: "说贵、他就给你算折扣",
    goal: "让他不再问、还不尴尬",
    constraint: "不能撒谎说要搬家"
  },

  outline: [                   // 仅简单档：练习前就展示
    "先说结论：这个数据不够硬",
    "再说为什么方案仍然成立",
    "最后给一个验证计划"
  ],
  hints: [                     // 简单和中等档：卡壳时展示
    "先承认局限，再讲为什么仍然值得做",
    "把「15%」换成一个更保守的表述",
    "主动提出上线后怎么验证"
  ]
}
```

`blocker`（你的卡点）是这套结构里最关键的字段。它把场景从「描述一件事」变成「解决一个困境」，而现实中让人说不出话的，恰恰是困境。

`assets` 里刻意放了对用户不利的信息（试验只跑了三天，还赶上大促），这借鉴了医学教育标准化病人剧本的「隐藏信息」设计：用户必须自己决定要不要主动交代。

### 5.5 场景卡的可读性准则

这一条比任何技术细节都重要：**场景卡的难点必须在「怎么说」，不能在「看不看得懂」。**

任何需要专业知识才能理解的信息，都把认知负担放错了地方。用户卡住不是因为不会表达，是因为没读懂题。这时候练习就失效了，而且他会觉得是自己笨。

三条硬规则：

1. **不用行业黑话和专业术语**。不写「置信区间不够」，写「那次试验只跑了三天，还赶上了大促」。同样是「这个数据不可靠」，后者任何人都能判断。
2. **数字要自带含义**。不写「留存率 23%」，写「一百个人里，第二天还会回来的只有二十三个」。
3. **不利信息要能用常识判断**。用户看一眼就该明白「这个地方不占理」，不需要先具备某个领域的知识。

一个自查方法：**把这张卡给一个完全不懂这行的朋友看，他能不能在十秒内说出「这人现在难在哪」。** 说不出来，这张卡就得重写。

个性化生成会天然引入用户所在行业的语境，这是好事。但「行业相关」不等于「行业黑话」，生成提示词里必须把这条写死。

### 5.6 场景卡怎么呈现

八个字段用散文一段段铺开，是一堵墙。第一眼就累，用户还没开口就先做了一次阅读理解。

#### 电报体，不是句子

每个字段存两份。`brief` 是**短语堆叠**，不是完整句子：

| 字段 | 不要这样写 | 要这样写 |
|---|---|---|
| 你是谁 | 你三十一岁，单身，工作在外地 | 三十一岁、单身、在外地 |
| 听众 | 姑姑，她是真心觉得在帮你 | 姑姑、真心觉得在帮你 |
| 卡点 | 说「太贵了」他会给你算折扣 | 说贵、他就给你算折扣 |

去掉主语、连词、语气助词，用顿号切开。目标是 **12 个字以内，一眼扫完**。

完整版仍然要存，但**不做点击展开**。多一次点击就是多一道摩擦，而这些信息本来就该一眼给全。完整版留给复盘时 AI 读，以及需要精确措辞的地方。

#### 画布式布局

不要用等宽网格，那样长短不一会显得散。用**商业模式画布那种拼图式布局**：格子紧贴，**没有边框也没有缝隙**，靠色块本身完成分区。

```
┌──────────┬────────────┬──────────┐
│ 什么场合  │            │  你是谁   │
├──────────┤  难在这里   ├──────────┤
│ 已经发生  │            │ 对面是谁  │
├──────────┤            ├──────────┤
│ 你已知的  │            │  要拿到   │
├──────────┴────────────┴──────────┤
│              不能做               │
└──────────────────────────────────┘
```

三条设计意图：

1. **卡点占据中间整列**，字号最大，用整张画布唯一的暖色。它在场景卡里的地位，等同于商业模式画布里的价值主张
2. **左列是客观情况**，**右列是人和目标**。左边回答「发生了什么」，右边回答「你要对谁做到什么」
3. **不能做横贯底部**，用灰粉色，像一条底线。它是边界，不是内容

实现上用 CSS Grid 的 `grid-template-areas`，`gap: 0`，格子不设边框。窄屏塌成单列，卡点排第一。

#### 分区命名

标签一律用**口语动词短语**，不用抽象名词。用户是在准备开口说话，不是在填表。

| 字段 | 不要用 | 用 |
|---|---|---|
| situation | 情境 | 什么场合 |
| facts | 已发生 | 已经发生 |
| assets | 你手上有 | 你已知的 |
| blocker | 卡点 | 难在这里 |
| audience | 听众 | 对面是谁 |
| goal | 目标 | 要拿到 |
| constraint | 限制 | 不能做 |

「你手上有」这类说法带有筹码和博弈的味道，对日常场景太重。「你已知的」更中性，也更准确：这一格装的本来就是用户掌握的信息，包括对自己不利的那部分。

#### 配色：莫兰迪

八个格子各一个低饱和灰调色，靠色相区分而不是靠线条。

| 格子 | 色值 | 色感 |
|---|---|---|
| 什么场合 | `#DDE3E5` | 浅灰蓝 |
| 已经发生 | `#C9D2D6` | 深灰蓝 |
| 你已知的 | `#D1D9CD` | 灰绿 |
| 难在这里 | `#DDC3B2` | 灰陶，全场唯一的暖色 |
| 你是谁 | `#EBE5DB` | 浅米 |
| 对面是谁 | `#DCD4C8` | 深米 |
| 要拿到 | `#E5D9BB` | 灰黄 |
| 不能做 | `#E0CFC9` | 灰粉 |

文字用 `#463F37`，标签用 `#5A5248`，卡点格里换成 `#6B4030`。所有组合的对比度都在 4.5 以上，最低的一组是卡点标签的 4.58。

**主按钮不要用黑色。** 黑色在这套低饱和配色里会显得突兀且廉价。用陶土色 `#96604A`，白字对比度 5.16。

#### 不做逐条揭示

早期版本试过「一条一条点开」的 RPG 式推进，实测下来是**用仪式感换速度，不划算**。用户想看的是全局，不是被喂食。游戏化应该加在练习和记录上，不该加在读题上。

### 5.7 难度分级

难度不只是「给不给提示」，**题目本身也要跟着变**。只调脚手架不调场景，难度就是假的。

| 维度 | 简单 | 中等 | 困难 |
|---|---|---|---|
| 卡点数量 | 1 个 | 1 个卡点加 1 条限制 | 2 个卡点叠加 |
| 听众立场 | 友好，愿意听 | 中立，讲清楚就行 | 有敌意或利益对立 |
| 信息完整度 | 全给，照着讲就行 | 有一处模糊 | 有缺口，得自己承认不知道或主动澄清 |
| 练习前 | 展示三段式提纲 | 只给一句方向 | 什么都不给 |
| 卡壳时 | 停顿 2.5 秒给提示 | 停顿 3.5 秒给提示 | 不给提示 |
| 时长 | 60 秒 | 60 秒 | 90 到 120 秒 |

**困难档的时长要更长，不是更短。** 一分钟可以靠冲，两分钟必须有结构。缩短时长只会制造慌乱，拉长才真正考验组织能力。

三档的场景差异举例，同一个人在同一个处境下：

- **简单**：同事问起你手上的项目，你要讲清楚现在做到哪了
- **中等**：主管问项目进展，你要说清楚延期两周这件事
- **困难**：主管上周刚在会上说过这个项目一切顺利，现在你要当着他和另外两个部门的面说要延期，同时争取加人

### 5.8 难度怎么选

**默认跟着能力走，但随时可以手动切。**

- 新用户前 5 张强制从简单开始。泛人群最怕的是第一次就被打击，先建立「我能讲完六十秒」的信心。
- 之后按最近 5 次的平均分推荐：低于 6 分推简单，6 到 8 分推中等，高于 8 分推困难。
- 抽题页始终有三个难度按钮，用户想挑战或想放松都随时可以。

**评分必须带难度标签**，否则用户会一直待在简单档刷高分。复盘页显示「困难档 6.5 分」而不是光秃秃的 6.5，记录页的折线也要按难度分色。让「困难档 6 分」看起来比「简单档 9 分」更值得骄傲，这是个态度问题。

### 5.9 生成时的难度配比

一轮 50 张的建议配比：

| 难度 | 张数 | 理由 |
|---|---|---|
| 简单 | 15 | 状态不好的日子也能练，不至于放弃 |
| 中等 | 25 | 主力区间 |
| 困难 | 10 | 够用就行，练不动的时候堆着反而有压力 |

首批 12 张要覆盖三个难度，不能全是简单，否则用户会以为这个产品很浅。建议首批为 5 简单、5 中等、2 困难。

第二轮生成时，如果用户困难档的完成率低于 50%，就把配比调向中等，别硬推。

### 5.10 分批生成策略

一次生成 50 张完整场景卡，即使流式输出也要一分钟以上，用户只能干等。

改成两批：

| 批次 | 数量 | 时机 | 用户感知 |
|---|---|---|---|
| 首批 | 12 张 | 建档提交后立即 | 流式逐张出现，约 15 秒后即可开练 |
| 补批 | 38 张 | 首批完成后台触发 | 无感，练到第 8 张时早已补完 |

第二轮的触发同样不需要用户操作：**剩余未练题数低于 5 时静默生成**。用户永远不会遇到「题练完了」的空状态。

单张场景卡约 300 个中文字符，50 张约 15000 字符。分成 5 次请求、每次 10 张，可以避免单次响应过长导致的截断和 JSON 解析失败。具体费用请按你的网关计费方式自行核算，我不掌握实时价格。

### 5.11 生成提示词骨架

```
你在为一个口语表达练习工具生成场景卡。

用户画像：
- 角色：{role}
- 行业：{industry}
- 年限：{years}
- 想改善：{painPoints}
- 补充：{note}

生成 {n} 张场景卡，要求：

1. 每张卡必须包含一个具体的困境，而不只是一个话题。
   反例：「介绍一下你负责的项目」
   正例：「你的项目延期了，而主管上周刚在会上说过一切顺利」

2. 卡点必须来自用户勾选的 {painPoints}，每个卡点至少覆盖 {n/3} 张。

3. facts 字段必须包含具体数字，assets 字段必须包含一条对用户不利的信息。

4. 场景要贴合 {industry} 的真实工作方式，不要写成通用职场剧。

5. goal 必须是一个可判断达成与否的目标，不能是「表达清楚」这种空话。

6. 按 15 简单 / 25 中等 / 10 困难 的配比生成，difficulty 字段标明。
   简单档：1 个卡点，听众友好，信息给全，另附三段式 outline。
   中等档：1 个卡点加 1 条限制，有一处信息模糊。
   困难档：2 个卡点叠加，听众有敌意或利益对立，信息有缺口，不提供 outline。

7. 每个字段都要额外给一个 brief 版本，12 个中文字以内，
   写成短语堆叠而不是句子，用顿号切开，去掉主语和连词。
   例：不写「你三十一岁，单身，工作在外地」，
   写「三十一岁、单身、在外地」。

8. 全卡不得出现专业术语、行业黑话、需要专门知识才能理解的指标名。
   数字必须自带含义，assets 里的不利信息必须能用常识判断。
   检验标准：一个完全不懂这行的人读完，能在十秒内说出「这人现在难在哪」。

严格返回 JSON 数组，不要 markdown 代码块，每个元素包含：
situation, role, audience, facts, assets, blocker, goal, constraint,
title, scenario, painPoint, hints（3 条）
```

第二轮生成时，额外注入用户的练习历史：**上一轮里哪些卡点得分低，下一轮就多出这一类**。这让题库随着练习自我调整，而不是简单地再来 50 道。

### 5.12 审核页

- 卡片列表，每张显示 title、scenario 标签、painPoint 标签、blocker 一句话
- 默认全部 Approve，右上角一个开关切换状态，点掉即变灰
- 顶部：「已选 47 / 50」和两个按钮：「加入我的题库」「先不看了，直接开练」
- 首批 12 张流式出现，补批到达时追加，不打断浏览

### 5.13 复盘要说什么

**这一节是整个产品最容易做废的地方。**

一个反例，来自 demo 的实测：用户认真讲了 212 字，拿到的总评是「开头直接给了结论，这次比上次稳」。这句话和他讲了什么毫无关系。同一句话，他阴阳怪气地讲、认真地讲、胡言乱语地讲，都会出现。

**分数加一句正确的废话，等于没有复盘。** 用户能立刻分辨出来，而且分辨出来之后就不会再回来了。

复盘必须做到三件事，缺一件都不成立。

#### 一、引用原话，指出具体哪句可以改

不是「表达不够简洁」，是把用户说过的那一句抠出来，说清楚它为什么给自己挖了坑，然后给一个可以直接替换的说法。

实例：

> 你说了「包括经济能力」。
> 这四个字是这次唯一的硬伤。卡点写得很清楚：说贵他就给你算折扣。
> 你前面已经找到了「我自己练」这个和钱无关的理由，中间又把经济能力说出来，
> 等于把梯子递回给他。删掉这四个字，防守就是完整的。

好的点评要能落到**具体的删改动作**上。「删掉这四个字」是可执行的，「注意用词」不是。

同时必须点出**讲得好的那一句**，并说明为什么好。用户需要知道哪个动作值得重复。

#### 二、诊断有没有回答到对方的要点

场景卡里写了 `audience`（听众关心什么）、`blocker`（卡点）、`goal`（要达成什么）。复盘要逐项对照：

| 诊断项 | 判定 | 说明什么 |
|---|---|---|
| 卡点 | 正面处理 / 绕开了 / 反被将军 | 用户有没有碰那个真正难的地方 |
| 目标 | 达成 / 部分达成 / 未达成 | 对照 `goal` 逐条判断 |
| 听众要点 | 回应了 / 答非所问 | 对方真正关心的事有没有被接住 |
| 限制 | 遵守 / 越界 | 有没有踩 `constraint` 里的红线 |

「部分达成」要说清楚**部分在哪**。上面那个例子里，「让他不再问」只是部分达成，因为留了两个口子；「以后见面不尴尬」是完全达成。

#### 三、站在对方视角做追问预演

这是最有价值的一块，也是别的产品没有的。

对方看完你这段话，下一句会问什么？**而且每一条追问都必须能追溯到用户的原话**，不能凭空生成。

实例：

> 1. 「年卡摊下来比月卡便宜，你算过吗」（因为你提了经济能力）
> 2. 「自己练容易受伤，我只带你几次」（因为你说了「实在不行也会想办法」）
> 3. 「那你月卡还续吗」（你全程没说以后还来不来）

第三条是**用户完全没防的**，而它恰恰和目标里「你还得继续来这家健身房」直接冲突。找出这种盲区，是复盘最大的价值。

追问预演还有一个副产品：它天然构成下一轮练习。用户可以直接选一条追问，立刻再讲一次。

#### 输出结构

```json
{
  "overall": "一句话总评，30 字内，必须点到这次的具体特征",

  "quotes": [
    {
      "said": "用户的原话片段，逐字引用",
      "kind": "问题 / 亮点",
      "why": "为什么这句有问题或为什么好",
      "better": "可以直接替换的说法，亮点则留空"
    }
  ],

  "diagnosis": {
    "blocker":    {"verdict": "正面处理", "why": "..."},
    "goal":       {"verdict": "部分达成", "why": "..."},
    "audience":   {"verdict": "回应了",   "why": "..."},
    "constraint": {"verdict": "遵守",     "why": "..."}
  },

  "followUps": [
    {
      "q": "对方可能追问的原话",
      "trigger": "因为你说了「……」",
      "prep": "下次可以这样接"
    }
  ],

  "scores": {"结构": 7, "内容": 8, "表达": 7, "用上背景": 6, "回应卡点": 7},
  "rewrite": "示范重讲，80 字左右，用用户自己的语气和素材，不要另起炉灶"
}
```

`quotes` 里至少一条问题、一条亮点。`followUps` 三条，其中至少一条要是用户完全没防到的。

#### 复盘提示词

```
你是一个表达教练。用户刚刚对着下面这张场景卡讲了一段话，
请给出复盘。

场景卡：
  情境：{situation}
  他是谁：{role}
  听众：{audience}
  已发生：{facts}
  他手上有：{assets}
  卡点：{blocker}
  要达成：{goal}
  限制：{constraint}

用户实际说的（语音转写，可能有错别字，按意思理解，不要点评错别字）：
「{transcript}」

要求：

1. 必须逐字引用用户的原话。禁止出现「表达不够简洁」
   「逻辑可以更清晰」这类放在谁身上都成立的评语。
   每条点评都要能落到具体的删改动作上。

2. 至少指出一条问题和一条亮点。
   亮点要说明为什么好，让他知道这个动作值得重复。

3. 逐项诊断卡点、目标、听众要点、限制，
   「部分达成」必须说清楚部分在哪。

4. 追问预演三条。每一条都必须能追溯到用户的原话，
   在 trigger 里写明是他说的哪句给了对方这个口子。
   其中至少一条要是他完全没防到的盲区。

5. 语气是教练不是考官。可以直说问题，但不要评判这个人。
   不要用「你应该」，用「下次可以」。

6. 如果用户几乎没说话，或者说的内容和场景完全无关，
   不要硬凑点评。直接说明这一点，scores 全部给 3 分以下，
   followUps 留空。

7. 用户可能带情绪，比如阴阳怪气或者明显在敷衍。
   照实指出来，这本身就是最值得复盘的地方。

严格返回 JSON，不要 markdown 代码块。
```

第 7 条是必须写的。用户会拿情绪化的回答来试探这个产品是不是真的在听，**这是他判断产品可信度的第一道测试**。

### 5.14 练习记录

这是泛人群留存的核心，优先级不低于题库本身。

```js
// 单次练习记录
{
  id, cardId, date,
  transcript,                        // 完整转写
  durationSec,
  scores: { 结构: 7, 内容: 6, 表达: 8, 用上背景: 5, 回应卡点: 4 },
  overall, highlight, improvements, rewriteExample,
  fillerWords: [{ word: "然后", count: 5 }]
}

// 每日汇总
{ date, count, totalSec, avgScore }
```

复盘页新增两个只有场景卡才有的维度：

- **用上背景**：给了「样本量只有 2000」这个数字，用户讲的时候用了吗
- **回应卡点**：卡点是「答不上来又不能说不知道」，用户正面处理了还是绕开了

记录页要展示的：

- 连续练习天数（最显眼的一个数字）
- 本周练习分钟数
- 最近十次的平均分折线，按难度分色
- 按题目回看：题干、当时说了什么、评分、建议
- 高频口癖词排行

### 5.15 数据存放

云端是权威数据源，localStorage 降级为离线缓存。详见第八章。

未登录时可以试用通用题库，但个人题库、练习记录、连续天数都需要登录后才可用。这是必要的取舍：这些数据一旦丢失，用户积累的进步感就没了，而这恰恰是泛人群留下来的唯一理由。

### 5.16 场景卡示例

**示例一：工作汇报**

> **情境**　周三下午的跨部门对齐会，你有五分钟
> **你是谁**　数据平台组的工程师，入职八个月
> **听众是谁**　三个业务方的负责人，他们只关心自己的需求什么时候能排上
> **已经发生了什么**　你们组这个季度收到 23 个需求，只完成了 9 个，积压 14 个
> **你已知的**　积压的需求里有 6 个是重复的，实际只有 8 个独立需求；组里两个人上个月离职了
> **你的卡点**　说人手不够像在找借口，说需求重复像在指责业务方
> **这次要达成什么**　让三方接受一个统一的需求提报流程，而不是当场吵起来
> **限制**　五分钟，不能点名批评任何一方

**示例二：求职面试**

> **情境**　三轮面试的最后一轮，对面是用人部门总监
> **你是谁**　工作四年，前一份工作做到一半公司裁撤了整个部门
> **听众是谁**　总监，他手上有两个候选人，另一个背景比你光鲜
> **已经发生了什么**　他刚问完：「你上一段工作只做了十一个月，为什么这么短？」
> **你已知的**　你在那十一个月里独立完成了一次从 0 到 1 的产品上线，日活做到 3 万
> **你的卡点**　真实原因是公司战略调整，但直接说「被裁了」会显得被动
> **这次要达成什么**　把这段经历讲成「短但密度高」，而不是「不稳定」
> **限制**　两分钟内，不能贬低前公司

**示例三：观点闲聊**

> **情境**　朋友聚餐，有人抛出了一个话题，大家都看向你
> **你是谁**　在座唯一在互联网公司做 AI 相关工作的人
> **听众是谁**　三个不同行业的朋友，其中一个刚被 AI 工具替掉了一部分工作，情绪不太好
> **已经发生了什么**　刚才有人半开玩笑地说：「你们做这个的，是不是在砸别人饭碗？」
> **你已知的**　你确实了解这个行业，也确实看到过裁员
> **你的卡点**　说「技术是中立的」太轻飘，说「确实会」又太扫兴
> **这次要达成什么**　给出一个真诚的、不回避也不说教的回答，让气氛不至于僵掉
> **限制**　一分钟，这是饭桌不是辩论赛

### 5.17 内置题库的定位变化

有了个人题库，原有的三十道通用题不再是主线，改为两个用途：

1. **未建档时的试用**。用户想先试一下再决定要不要填资料，给他一道通用题
2. **每日随手练**。「今天随便练一道」入口从通用题库抽，不消耗个人题库

## 六、动机设计

### 6.1 真正的问题不是没有工具

一个真实的自我观察，值得写进文档：

> 我知道可以和 AI 语音助手聊，但我就是不想打开它。我自己也加了订阅、设了定时任务，每天给我发消息让我对着几个问题回答，但我就是没那个习惯。

这句话里没有一个字是在抱怨工具不好。工具都有，提醒也有，就是不做。

所以问题不在功能，在**启动那一刻的摩擦**。打开语音助手要经过三道坎：

1. 打开 App
2. 想今天聊什么
3. 组织出第一句话

每一道都需要主动决策，而人在疲惫的时候最不愿意做的就是决策。定时提醒解决不了这个，**提醒只是把「该开始了」这个念头推到眼前，三道坎一道没少。**

对照来看，多邻国真正的核心机制不是经验值和排行榜，那些是留存放大器。它的核心是**把启动成本压到接近零**：一课两分钟，点开就有题，不用想今天学什么。

所以动机设计的第一优先级不是加分数，是**砍掉开始之前的所有决策**。

### 6.2 第一层：把启动成本砍到零

这一层做不好，后面几层全是白搭。

| 措施 | 具体做法 |
|---|---|
| 打开即练 | 首页第一屏就是一张卡加一个录音按钮，没有任何选择。想挑难度或换题的入口放在次要位置 |
| 通知带题面 | 推送不写「该练习了」，直接写题：「主管上周刚说过项目一切顺利，现在你要说延期。点开就能讲」。看到题的瞬间脑子已经开始想了，这就绕过了第二道坎 |
| 点开即录 | 从通知点进来直接进入录音准备状态，倒数三秒自动开始，不需要再点一次「开始」 |
| 30 秒版 | 状态不好的日子，30 秒也算完成。宁可让人练得浅，不要让人今天不练 |
| 断点续练 | 上次没讲完的题保留，回来直接接上，不用重新抽 |

**「30 秒也算完成」这条最容易被砍掉，但它可能是留存贡献最大的一条。** 习惯的敌人从来不是强度不够，是中断。

### 6.3 第二层：让停下来有代价

连续天数是主指标，放在最显眼的位置。它有效的原因不是数字好看，是**断了会痛**。

但连胜有个著名的暴政：断一次，人就再也不回来了。必须提前把这个坑填上。

| 机制 | 设计 |
|---|---|
| 补签 | 每坚持 7 天自动获得 1 个补签机会，最多存 3 个。断签时**自动消耗，不需要用户操作**，第二天回来只看到「用掉了一个补签，你还在连胜中」 |
| 请假 | 主动声明要休息几天，期间不算断。出差、生病、考试都该被允许 |
| 断了之后 | 不显示「连胜归零」，显示「上次连了 23 天，这次从头开始」。把成绩留在那里，而不是抹掉 |

最后这条是态度问题：**用户已经断了，产品不该再补一刀。**

### 6.4 第三层：让进步看得见

这是表达练习相对语言学习的优势：反馈即时且客观，说完立刻能看到自己说了什么。

- **口癖词下降曲线**。最有说服力的一条。「然后」从每分钟 8 次降到 2 次，这是硬数据，不是感觉
- **今昔对比**。同一张卡隔一个月再练一次，两段转写并排放。这个瞬间的冲击力比任何分数都大
- **难度晋级**。连续三次困难档过 7 分，解锁一个新档位。让升级来自能力而不是时长
- **里程碑卡片**。第 1 次、第 7 天、第 30 天、第 100 次，各给一张可保存可分享的卡

### 6.5 第四层：小星承担陪伴

角色是这个产品独有的资产，多邻国的猫头鹰是后来才补上的人格，你一开始就有。

- 小星的精神状态跟着连胜走。连得久，它站得更直、星星棒挥得更起劲
- 回来的时候小星是**高兴**的，不是责备的
- 里程碑时它换一个特别状态，比如第一次连满 30 天放一次满屏星星

**明确不做的：道德绑架。** 不做「你三天没来，小星哭了」这种设计。它短期有效，长期让人反感，而且和产品原则里的「不评判、不打扰」直接冲突。角色是陪你的，不是管你的。

### 6.6 明确不做的几件事

游戏化最容易做过头，这几样是主动放弃的：

| 不做 | 原因 |
|---|---|
| 排行榜 | 语言学习可以比谁背得多，表达能力比不了。横向比较会伤人，而且第一优先人群是最不自信的那批 |
| 生命值 / 体力 | 惩罚练习本身是荒谬的。多邻国的红心机制争议很大，别抄 |
| 高频推送 | 每天最多一条。第二条不会带来第二次练习，只会带来一次关闭通知 |
| 付费买连胜 | 一旦连胜能用钱买，它就不再是成就 |
| 社交比较 | 可以有「今天有 137 个人也在练」这种弱在场感，但不要有「你的朋友小李练了 30 天」 |

### 6.7 分数要服务于自我认知

一个容易出错的地方：**外在奖励会挤出内在动机**。如果用户是为了拿分而练，分数一停他就停了。

所以分数的呈现方式要往「我看到自己在变好」上引，而不是往「我要拿分」上引：

- 分数永远带难度标签。「困难档 6.5」看起来要比「简单档 9.0」更值得骄傲
- 复盘的第一行是那句总评，不是数字。数字放在下面
- 记录页的主视觉是趋势，不是当前分数

### 6.8 落地优先级

不要一次全做。按对留存的贡献排序：

1. **打开即练 + 通知带题面 + 30 秒版**（第一层，砍摩擦）
2. **连续天数 + 自动补签**（第二层，防流失）
3. **口癖词下降曲线 + 今昔对比**（第三层，看得见）
4. **小星状态跟随连胜**（第四层，情感）
5. 里程碑卡片、难度晋级、弱在场感（锦上添花）

如果只能做一件事，做第一件。**其余所有机制都建立在「用户真的开始了」这个前提上。**

## 七、通用题库现状与扩充规划

个人题库上线后，这三十道通用题不再是主线，只承担两个用途：未建档用户的试用，以及「今天随便练一道」的零决策入口。扩充仍有价值，但优先级低于个人题库。

### 7.1 现状

| 场景 | 现有题数 | 类型 | 问题 |
|---|---|---|---|
| 工作汇报 | 10 | 裸题 | 偏「向上汇报」，缺横向和向下沟通；全部缺背景信息 |
| 求职面试 | 10 | 裸题 | 偏行为面试经典题，缺压力题和反问环节 |
| 观点闲聊 | 10 | 裸题 | 偏严肃议题辩论，缺轻松的日常表达 |

三十道题全部没有背景信息，这是场景练习模块要补的核心缺口。

### 7.2 扩充方向

**工作汇报**（目标 30 题 + 10 张场景卡）

缺口在于现有题目几乎都是「你对上级说」。需要补：

- 横向：跨部门争取资源、拒绝一个不合理的需求、协调两个吵起来的团队
- 向下：给团队宣布一个不受欢迎的决定、给新人讲清楚一件复杂的事
- 特殊时刻：述职答辩、临时被 cue 发言、给客户解释一个我方失误
- 坏消息类：这一类最需要场景卡，因为卡点全在人际关系里

**求职面试**（目标 30 题 + 10 张场景卡）

现有十题是标准行为面试题，网上到处都有。差异化要往这些方向走：

- 难堪题：空窗期、频繁跳槽、被裁、绩效不好、转行动机
- 反问环节：轮到你问面试官时问什么（几乎没人练过，但每场都会遇到）
- 薪资沟通：怎么开口谈期望薪资、怎么应对压价
- 专业深挑：面试官对你的项目连问三个为什么
- 群面：怎么在八个人里既不抢话也不隐形

**观点闲聊**（目标 30 题 + 10 张场景卡）

现有十题偏「社会议题辩论」，对泛人群偏重。需要补轻的：

- 讲故事：讲一次尴尬经历、讲一个你很喜欢的地方、讲一次改变你的对话
- 社交场合：破冰自我介绍、婚礼或年会即兴发言、向长辈解释你的工作
- 有人际压力的：安慰一个刚失业的朋友、拒绝一个不想去的邀约、点评朋友给你看的作品

最后这一组和场景练习结合最好，因为它们的难点全是「话怎么说」而不是「观点是什么」。

### 7.3 优先级建议

考虑到第一优先人群是泛人群：

1. 先补观点闲聊的轻量题（对新用户最友好，开口成本最低）
2. 再做工作汇报的场景卡（使用频次最高，场景价值最明确）
3. 求职面试放最后（需求强但人群窄，且现有十题已经能覆盖高频需求）

---

## 八、账号与云端同步

### 8.1 为什么现在就要做账号

表面理由是换设备能看到自己的数据。真正的理由有三个，按紧迫程度排列：

1. **成本控制**。题库生成一次要调 50 张场景卡，每次练习还要一次复盘。密钥现在在前端，任何人打开控制台就能拿走随便刷。账号加后端代理加按用户限流是同一件事，不是三件。
2. **数据不能丢**。连续练习天数和进步曲线是泛人群留下来的唯一理由。localStorage 清一次缓存就没了，用户不会原谅第二次。
3. **容量**。localStorage 上限约 5MB，200 次练习的完整转写就会顶到天花板。

所以账号的优先级要提到 P0，和密钥迁移一起做。

### 8.2 选型约束

用户主要在中国大陆，这条直接排除了一批常见方案：

| 方案 | 为什么不选 |
|---|---|
| Supabase | 大陆访问受 GFW 影响不稳定，官方无中国区节点 |
| Firebase | 同上，且部分服务在大陆不可用 |
| Vercel 托管后端 | 大陆访问慢且不稳定 |
| Cloudflare Workers | 大陆链路质量波动大 |

讯飞和 DeepSeek 的服务本来就在境内，后端放在境内或香港，链路一致反而更快。

### 8.3 推荐方案

**一台轻量云主机，跑现有的 Node 服务，数据库用 SQLite。**

理由是你已经有 `server/index.js` 了，它现在只做讯飞签名。把它扩成完整后端，鉴权、AI 代理、数据同步全在一个进程里，不引入任何新的运维对象。

```
浏览器
  │
  ├─ 静态资源（Vite 打包产物，可放对象存储或同机 nginx）
  │
  └─ 单个 Node 服务
       ├─ /api/auth/*        邮箱验证码登录
       ├─ /api/xfyun/*       讯飞签名（已有）
       ├─ /api/ai/generate   题库生成代理，密钥在服务端
       ├─ /api/ai/review     复盘代理，密钥在服务端
       └─ /api/sync/*        数据读写
              │
              └─ SQLite 单文件
```

SQLite 的取舍要说清楚：**单文件、零运维、不需要单独的数据库进程**，几千用户完全够用。代价是只能单机、写入串行。但你现在的规模离触及这些限制还很远，而且只要 SQL 写得标准，将来迁 Postgres 基本不用改业务代码。

托管服务和自建数据库的方案也能用，但对「先最简单的，能跑就行」这个目标，多引入一个平台就是多一份心智负担。

### 8.4 ICP 备案

这是硬性的，绕不过去，必须提前说：

**自己的域名指向境内服务器，必须完成 ICP 备案。** 个人备案通常需要一到三周，期间网站不能对外提供服务。

务实的做法是分两步：

1. **现在**：用香港或新加坡的轻量服务器，不需要备案，当天就能跑通全流程。大陆访问比欧美节点好得多，做开发和小范围试用完全够。
2. **要公开推广时**：再申请备案并迁到境内。届时业务代码不用动，只迁数据和改 DNS。

不要为了省事把「以后再说」变成「上线后被关停」。

### 8.5 登录流程（邮箱验证码）

选验证码而不是密码，是因为**不存密码就没有密码泄露风险**，也不用做找回密码的流程。对一个个人项目来说，这是安全性和工作量都更优的选择。

```
1. 用户输入邮箱
2. 服务端生成 6 位数字码，存哈希，5 分钟过期
3. 发信
4. 用户输入验证码
5. 校验通过，签发 session token（32 字节随机），存哈希，30 天过期
6. token 放 httpOnly + Secure + SameSite=Lax 的 Cookie
```

必须做的四道防护，缺一个就会被刷：

- 同一邮箱 60 秒内只能请求一次验证码
- 同一邮箱每天最多 10 次
- 同一 IP 每小时最多 20 次
- 单个验证码最多试错 5 次，超过即作废

发信服务用阿里云邮件推送或腾讯云 SES，都有免费额度，个人项目量级用不完。注册和配置需要你自己完成，我不能代你填账号信息。

### 8.6 数据表

```sql
users            (id, email, created_at, last_seen_at)
login_codes      (email, code_hash, expires_at, attempts)
sessions         (token_hash, user_id, expires_at, created_at)

profiles         (user_id, role, industry, years, pain_points, note, updated_at)

cards            (id, user_id, round, approved, practiced, difficulty,
                  scenario, pain_point, title,
                  situation, role, audience, facts, assets,
                  blocker, goal, constraint_text, outline_json, hints_json,
                  created_at, updated_at)

records          (id, user_id, card_id, difficulty, practiced_at, transcript,
                  duration_sec, scores_json, overall, highlight,
                  improvements_json, rewrite_example, filler_words_json,
                  created_at)

daily_stats      (user_id, date, count, total_sec, avg_score)

usage_quota      (user_id, date, gen_calls, review_calls)
```

`usage_quota` 是省钱的关键。建议初始额度：**每人每天最多 2 次题库生成、30 次复盘**。超过就提示明天再来。这个数字远高于正常使用（正常人一天练三五次），但能挡住脚本刷。

容量估算：一次练习的转写约 600 字，折合 1.8KB。一个用户练一千次约 1.8MB。一千个活跃用户不到 2GB，SQLite 毫无压力。

### 8.7 同步策略

不做实时同步，做**登录时拉取，变更时上传**。

| 时机 | 动作 |
|---|---|
| 登录成功 | 全量拉取 profile、cards、最近 50 条 records、daily_stats，写入 localStorage |
| 生成题库 | 服务端直接落库，前端拿到结果后更新本地缓存 |
| 剔除某张卡 | 立即 PATCH，失败则入本地队列 |
| 练习完成 | 立即 POST 记录，失败则入本地队列 |
| 网络恢复 | 队列补传 |

**冲突处理用 last-write-wins 就够了。** 场景是同一个人在自己的几台设备上用，不是多人协作，每条记录带 `updated_at`，服务端取较新的那条。为这个场景引入 CRDT 或版本向量是过度设计。

localStorage 的角色变成：**离线可读缓存 + 上传失败队列**。清掉不会丢数据，重新登录就回来了。

### 8.8 隐私声明需要改写

现有的「本地优先，不收集用户数据」在有账号之后就不再成立，必须如实修改，不能含糊：

- 会存到服务器：邮箱、建档信息、场景卡、练习转写文本、评分
- 不会存：录音文件（讯飞传输后不落盘）、简历原文（仅浏览器端解析，只上传 AI 提炼后的摘要）
- 提供：导出全部数据为 JSON、注销账号并删除全部数据

「注销即删除」要真的实现，不是软删除。这是对用户最基本的尊重，也是合规要求。

### 8.9 迁移路径

现有本地用户的数据不能丢：

1. 首次登录时检测 localStorage 里有没有旧数据
2. 有就提示「发现本机有 N 次练习记录，要合并到这个账号吗」
3. 用户确认后上传，服务端按 `created_at` 去重
4. 合并完成后清理本地旧 key

## 九、自带密钥（可选）

### 9.1 定位

允许用户填自己的讯飞和 AI 密钥，但**不作为默认，也不作为省钱手段**。

先说清楚它解决不了什么。成本不是问题：一次复盘约输入 800 token、输出 400 token，一个用户一天练 5 次、一个月 150 次约 18 万 token，加上两轮题库生成约 3 万，合计一个月 21 万 token 左右。在 DeepSeek 这个档位的定价下这是很小的钱。真正的风险是脚本刷，而脚本刷用登录加限流就挡住了。

也别把它当默认。第一优先人群是泛人群，让他们去开放平台注册开发者、创建应用、开通服务、复制三个密钥再回来粘贴，绝大多数人会在第一步走掉。这和「十五秒开口」是根本冲突的。而且用户把密钥交给你之后，加密存储、防泄漏、防越权的责任全在你身上，这比自己管一把密钥的责任更大。

### 9.2 它真正解决的问题：并发

讯飞新用户礼包按认证程度给最多一路 50 小时免费时长，有效期一年。

50 小时约等于 3000 次一分钟的练习，总量是够的。但「一路」指的是并发数为 1，**同一时刻只能有一个人在录音，第二个人连上来就会失败**。

这才是自带密钥的价值：它是并发瓶颈的泄洪口，不是成本方案。

具体并发额度需要去讯飞控制台确认。如果确实是 1 路，那么在加购并发之前，多人同时使用是不可能的，这件事的优先级高于自带密钥本身。

### 9.3 分层设计

| 层 | 面向谁 | 行为 |
|---|---|---|
| 默认 | 所有新用户 | 用平台额度，登录后限流，零配置 |
| 自带密钥 | 重度用户、想绕开限额的、开发者 | 设置页填自己的密钥，填了就走自己的 |
| 并发兜底 | 并发占满时 | 提示「现在有人在练，稍等一下」，并引导去填自己的密钥 |

### 9.4 密钥存储要求

四条，一条都不能省：

1. **存服务端，加密存储**。用 AES-256-GCM，主密钥只放环境变量，绝不进数据库。数据库被拖走也只能拿到密文。
2. **永不下发到浏览器**。所有调用仍然走后端代理。讯飞签名需要 APISecret，一旦放前端，任何一个 XSS 都能把用户的密钥偷走。
3. **前端只回显后四位**，形如 `****abcd`。填完不再显示完整值，要换就重填。
4. **保存前先验证一次**。拿密钥去调一次最便宜的接口，通了才存。否则用户填错了要等到练习失败才发现。

### 9.5 需要的字段

| 服务 | 字段 |
|---|---|
| 讯飞实时语音转写 | APPID、APIKey、APISecret |
| AI | 端点 URL、API Key、模型 ID |

AI 侧把端点和模型也开放出来，用户就能接自己习惯的任何 Anthropic 兼容服务，不锁死在某一家。

### 9.6 这不能替代后端

即使做了自带密钥，登录、后端代理、限流仍然都要做。它只是在第八章那套架构上加一个可选开关，不是它的替代方案。

## 十、技术架构

### 10.1 现状

**前端**

- React 19 + Vite 7，纯前端，无数据库，状态在内存，刷新即重置
- 语音转写：讯飞实时语音转写 WebSocket API。浏览器采集音频，降采样至 16kHz，PCM 编码后经 WebSocket 发送
- AI 接口：DeepSeek（StreamLake 网关，Anthropic 兼容格式，流式 SSE）
- 角色动画：自研 SVG 形变状态机。9 个状态，6 条路径全部走控制点精确插值，不依赖 morph 库

**后端**

- 轻量 Node.js HTTP 服务（`server/index.js`），唯一职责是生成讯飞 WebSocket 签名 URL，监听 `127.0.0.1:8787`

**文件结构**

```
speaking/
├── src/
│   ├── main.jsx                          入口
│   ├── speech-practice-v2-xfyun.jsx      主组件（全部 UI 与逻辑）
│   ├── speechAnalysis.js                 口语词检测与实时建议
│   ├── deepseekStream.js                 DeepSeek 流式 API 封装
│   ├── StarKid.jsx                       角色动画引擎
│   ├── FloatingStarKid.jsx               可拖动悬浮容器
│   ├── starkidStates.js                  9 状态路径数据
│   └── xfyun-client/                     讯飞转写客户端与 Hook
├── server/
│   ├── index.js                          签名服务
│   └── xfyunSigner.js                    讯飞签名算法
├── index.html
└── package.json
```

### 10.2 已知问题

按严重程度排列：

| 问题 | 影响 | 建议 |
|---|---|---|
| DeepSeek API Key 硬编码在前端 `deepseekStream.js` | 任何人打开控制台都能拿走，会产生真实费用 | 上线前必须迁移到 `server/` 做代理，这是唯一的阻塞项 |
| 总评脱离用户实际内容 | 只要总评是模板化的，用户一眼就能看穿，产品可信度归零 | 按 5.12 重构，强制引用原话 |
| 复盘评分维度不一致 | 提示词按场景给了四个差异化维度，但强制返回的 JSON 只有「结构 / 内容 / 表达」三个固定字段，差异化设计实际没有生效 | 让 `scores` 的键跟随场景动态生成 |
| 无任何持久化 | 刷新即丢，无法做连续天数和进步曲线，而这是泛人群留存的关键 | 先用 localStorage 存练习记录，账号体系后置 |
| DeepSeek 资源包已耗尽 | 复盘和卡壳提示均不可用 | 续费或切换模型 |
| 讯飞转写断线无重连 | 网络抖动时练习中断，体验挫败感强 | 加自动重连与降级提示 |
| 图标为手绘 SVG 路径 | 与项目自带的 taste-skill 规范冲突 | 换成 Phosphor 或 Tabler 图标库 |
| 移动端适配不完整 | 泛人群大量在手机上 | 优先级应随第一人群定位上调 |

---

## 十一、产品原则

1. **一分钟够了**。不要让用户觉得练口语是个大工程。一道题，一分钟，练完就走。
2. **AI 是教练，不是考官**。卡壳提示要温柔，复盘要具体可行，不给压力。
3. **量化但不焦虑**。分数是参考，重点是「这次比上次好在哪」。
4. **数据是用户的**。登录只为了同步和防刷，不为了追踪。随时可导出全部数据，注销即真删除，简历原文永远不离开浏览器。
5. **可爱不能变成负担**。角色可关闭，动画可降级，任何装饰性元素都不能挡住主任务。

---

## 十二、路线图

### P0（上线前必须）

- [ ] DeepSeek API Key 迁移到后端代理
- [ ] 邮箱验证码登录与 session
- [ ] SQLite 建表与数据同步接口
- [ ] 按用户限流（每天 2 次生成、30 次复盘）
- [ ] 隐私声明改写，提供导出与注销
- [ ] DeepSeek 资源包续费
- [ ] 复盘评分维度与场景对齐
- [ ] 移动端适配完善

### P1（第一个版本）

个人题库闭环，按依赖顺序：

- [ ] 建档页：结构化字段与内置提示词文案
- [ ] 场景卡数据结构与生成提示词（含 brief 要点字段）
- [ ] 场景卡呈现：画布式布局 + 电报体要点
- [ ] 难度三档：场景差异、脚手架差异、时长差异
- [ ] 难度推荐与手动切换，评分带难度标签
- [ ] 分批生成：首批 12 张流式可练，后台补 38 张
- [ ] 审核页：默认全 Approve，支持跳过
- [ ] 抽题页「跳过，别再出这题」
- [ ] 本地缓存与失败队列补传
- [ ] 老用户本地数据合并到账号
- [ ] 记录页：连续天数、本周分钟数、评分折线、按题回看
- [ ] 复盘重构：逐句点评、四项诊断、追问预演
- [ ] 复盘新增「用上背景」「回应卡点」两个维度
- [ ] 从追问预演一键进入下一轮练习
- [ ] 第二轮触发：剩余未练题数低于 5 时静默生成，并注入低分卡点
- [ ] 打开即练：首页首屏一张卡加一个录音按钮，零选择
- [ ] 通知带题面，点开直接进录音准备
- [ ] 30 秒版入口
- [ ] 连续天数与自动补签
- [ ] 讯飞转写断线重连
- [ ] 数据导出 JSON
- [ ] 确认讯飞并发额度，不足则加购

### P2（中期）

- [ ] 自带密钥：讯飞与 AI，加密存储、保存前验证、只回显后四位
- [ ] 手写自己的场景卡
- [ ] 练习时长可选（30 / 60 / 120 秒）
- [ ] 语音回放（录音保存与回听）
- [ ] 口癖词下降曲线与今昔对比
- [ ] 小星状态跟随连胜
- [ ] 里程碑卡片、难度晋级

### P3（长期）

- [ ] 多轮对话模式（AI 追问，模拟真实面试）
- [ ] 发音评测（语速、停顿、语调）
- [ ] AI 面试官模式
- [ ] 更多场景：演讲、销售话术、英语口语
- [ ] 社区与企业版

---

## 十三、待确认

1. 产品名是否采用「小星」，需要核实商标与域名可用性
2. 「最想改善什么」的十个选项文案是否覆盖了你自己的真实卡点，这批文案决定生成质量
3. 单轮 50 张是否合适，还是先做 30 张看完成率
4. 未建档用户能不能直接练，还是必须先填资料（影响首次转化）
5. 服务器先放香港（免备案，当天可跑）还是直接备案上境内
6. 复盘评分维度是否要随场景动态变化，还是统一成一套便于横向比较进步
7. 未登录用户能试用几道题，什么时候弹登录（影响首次转化）
8. 讯飞并发额度实际是几路，这决定了能不能多人同时练
9. 场景卡的可读性准则（5.5）需要你拿自己不懂的行业验一遍，看生成结果是否真的没有黑话
10. 「30 秒也算完成」会不会让练习流于形式，需要上线后看数据
11. 通知带题面涉及把练习内容推到锁屏，隐私上是否可接受，要不要做成开关

## 附：API 配置

**讯飞实时语音转写**

- 服务端环境变量：`XFYUN_APP_ID`、`XFYUN_API_KEY`、`XFYUN_API_SECRET`
- 签名服务地址：`http://127.0.0.1:8787/api/xfyun/realtime-url`

**DeepSeek（StreamLake 网关）**

- 端点：`https://wanqing.streamlakeapi.com/api/gateway/v1/messages`
- 模型：`ep-2wt0ee-1785658859439536998`
- 格式：Anthropic Messages API 兼容，流式 SSE

**安全与隐私**

- 简历文件仅在浏览器端解析，不上传服务器
- 只保存 AI 提炼后的重点摘要，不保存简历原文
- 语音数据经讯飞 WebSocket 传输，不在本地保存录音
