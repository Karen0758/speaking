# 开麦 - 产品原型文档

## 产品理念

**开麦**是一个帮助用户练习口语表达的 Web 工具。核心信念：表达能力不是天赋，是可以通过刻意练习提升的技能。

大多数人知道自己"不太会讲"，但缺少一个低压力、即时反馈的练习环境。开麦提供随机话题 + 限时练习 + AI 实时辅助 + 自动复盘的闭环，让用户每天花一分钟，就能持续打磨自己的表达。

### 核心价值主张

- **低门槛**：打开网页，抽题就练，不需要对练伙伴
- **即时反馈**：实时转写 + 口语词高亮 + AI 卡壳提示
- **有量化**：练完自动评分，看得见进步
- **场景化**：针对工作汇报、求职面试、日常表达三个高频场景分别优化

---

## 目标用户

1. **求职者**：准备面试，需要反复练习自我介绍和行为面试题
2. **职场人**：需要做汇报、述职、跨部门沟通，想提升结构化表达
3. **表达力提升者**：日常聊天容易卡壳，想让自己说话更有条理、更有趣

---

## 功能架构

```
开麦
├── 首页（选择场景）
│   ├── 工作汇报
│   ├── 求职面试
│   └── 观点闲聊
├── 抽题页
│   ├── 随机抽题（老虎机动画）
│   ├── 换题
│   └── 求职档案（面试场景专属）
│       ├── 目标岗位
│       └── 简历上传 → AI 提炼重点
├── 练习页（核心）
│   ├── 倒计时 60 秒
│   ├── 讯飞实时语音转写
│   ├── 口语词实时高亮
│   ├── AI 卡壳提示（停顿 3.5 秒自动触发，流式输出，用户开口即中断）
│   └── 麦克风音量可视化
└── 复盘页
    ├── AI 评分（结构 / 内容 / 表达，各 1-10 分）
    ├── 总评一句话
    ├── 口癖词统计
    ├── 亮点摘录
    ├── 改进建议
    ├── 示范重讲
    └── 完整转写文本
```

---

## 三个场景的差异化设计

| 维度 | 工作汇报 | 求职面试 | 观点闲聊 |
|------|----------|----------|----------|
| 核心框架 | 结论先行 | STAR 结构 | 观点 + 论据 |
| AI 提示风格 | 引导补结论/数据 | 引导结合自身经历 | 引导举例/回应反方 |
| 复盘评分维度 | 结构/信息密度/受众意识/流畅度 | 结构/具体性/相关性/流畅度 | 观点清晰度/论据/流畅度/趣味性 |
| 专属功能 | 无 | 求职档案（岗位+简历） | 无 |
| 话题类型 | 向上汇报/复盘/推进 | 自我介绍/行为面试 | 社会议题/生活选择/观点辩论 |

---

## 技术架构

### 前端

- **框架**：React 19 + Vite 7
- **语音转写**：讯飞实时语音转写 WebSocket API
  - 浏览器端采集音频 → 降采样至 16kHz → PCM 编码 → WebSocket 发送
  - 服务端签名生成 URL（避免暴露 API Secret）
- **AI 接口**：DeepSeek（StreamLake 网关，Anthropic 兼容格式，流式 SSE）
  - 卡壳提示：流式输出，AbortController 支持用户开口中断
  - 复盘评分：流式接收完整 JSON 后解析
  - 简历解析：流式逐字显示提炼结果
- **纯前端**：无后端数据库，状态存在内存中，刷新即重置

### 后端

- 轻量 Node.js HTTP 服务（`server/index.js`），唯一职责：生成讯飞 WebSocket 签名 URL
- 从 `.env` 读取讯飞 API 密钥，不暴露到前端
- 监听 `127.0.0.1:8787`

### 文件结构

```
speaking/
├── src/
│   ├── main.jsx                          # 入口
│   ├── speech-practice-v2-xfyun.jsx      # 主组件（全部 UI + 逻辑）
│   ├── speechAnalysis.js                 # 口语词检测 + 实时建议
│   ├── deepseekStream.js                 # DeepSeek 流式 API 封装
│   └── xfyun-client/
│       ├── xfyunRealtimeTranscriber.js   # 讯飞 WebSocket 转写客户端
│       └── useXfyunRealtimeTranscriber.js # React Hook 封装
├── server/
│   ├── index.js                          # 签名服务
│   └── xfyunSigner.js                   # 讯飞签名算法
├── index.html
└── package.json
```

---

## 核心交互流程

```
选场景 → 抽题 → 开麦练习（60秒）→ 自动复盘
                    │
                    ├── 讯飞实时转写 → 口语词高亮
                    ├── 停顿 3.5s → AI 流式提示
                    └── 用户开口 → 中断 AI 提示
```

---

## 产品原则

1. **一分钟够了**：不要让用户觉得"练口语"是个大工程。一道题，一分钟，练完就走。
2. **AI 是教练，不是考官**：卡壳提示要温柔，复盘要具体可行，不是给人压力。
3. **量化但不焦虑**：分数是参考，重点是"这次比上次好在哪"。
4. **本地优先**：不需要注册登录，不收集用户数据，简历在设备上解析。

---

## 后续规划

### P0（当前需要解决）

- [ ] DeepSeek API 资源包续费（当前已耗尽）
- [ ] 讯飞转写连接稳定性优化（断线重连）
- [ ] 移动端适配完善

### P1（近期迭代）

- [ ] 练习历史记录（localStorage 持久化）
- [ ] 练习次数统计 + 趋势图
- [ ] 自定义话题（用户自己输入题目）
- [ ] 练习时长可选（30s / 60s / 120s）

### P2（中期规划）

- [ ] 用户账号体系 + 云端同步
- [ ] 多轮对话模式（AI 追问，模拟真实面试）
- [ ] 语音回放（录音保存 + 回听）
- [ ] 发音评测（语速/停顿/语调分析）
- [ ] 更多场景：演讲、销售话术、英语口语

### P3（长期愿景）

- [ ] AI 面试官模式（AI 提问 → 用户回答 → AI 追问 → 复盘）
- [ ] 社区功能（优秀回答分享、排行榜）
- [ ] 企业版（团队培训、批量出题、数据看板）

---

## API 配置

### 讯飞实时语音转写

- 服务端环境变量：`XFYUN_APP_ID`、`XFYUN_API_KEY`、`XFYUN_API_SECRET`
- 签名服务地址：`http://127.0.0.1:8787/api/xfyun/realtime-url`

### DeepSeek（StreamLake 网关）

- 端点：`https://wanqing.streamlakeapi.com/api/gateway/v1/messages`
- 模型：`ep-2wt0ee-1785658859439536998`
- 格式：Anthropic Messages API 兼容，流式 SSE
- 注意：API Key 当前硬编码在前端 `src/deepseekStream.js`，生产环境应迁移到后端代理

---

## 安全与隐私

- 简历文件仅在浏览器端解析，不上传到任何服务器
- 只保存 AI 提炼后的重点摘要，不保存简历原文
- 语音数据通过讯飞 WebSocket 传输，不在本地保存录音
- DeepSeek API Key 当前在前端暴露，需在生产化时迁移到后端
